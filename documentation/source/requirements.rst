Requirements
============
1. conda env create -f environment.yml

2. source activate armr

3. open aws anaconda 3 ec2 medium instance

4. configure git

5. set AWS credentials correctly in `user_definition.py`
